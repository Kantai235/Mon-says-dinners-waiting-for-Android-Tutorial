<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kantai.androidexampledemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Ch01Activity" />
        <activity android:name=".Ch02Activity" />
        <activity android:name=".Ch03Activity" />
        <activity android:name=".Ch04Activity" />
        <activity android:name=".Ch05Activity" />
        <activity android:name=".Ch06Activity"></activity>
    </application>

</manifest>